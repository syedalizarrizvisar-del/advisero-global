import AppointmentBookingPageClient from "@/components/booking/AppointmentBookingPageClient";

export const metadata = {
  title: "Book Appointment | Advisero Global LLC",
  description: "Schedule a premium international consultation with Advisero Global LLC using our professional appointment booking system.",
};

export default function AppointmentPage() {
  return <AppointmentBookingPageClient />;
}
